name: Backend Pipeline

on:
  push:
    branches: [ deploy ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker Image
        run: docker build -t felipensio/backend_intro_devops_casino:latest .

  push:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          
      - name: Build and Push Tags
        run: |
          docker build -t felipensio/backend_intro_devops_casino:latest .
          docker tag felipensio/backend_intro_devops_casino:latest felipensio/backend_intro_devops_casino:v1.0.0
          docker tag felipensio/backend_intro_devops_casino:latest felipensio/backend_intro_devops_casino:sha-${{ github.sha }}
          
          docker push felipensio/backend_intro_devops_casino:latest
          docker push felipensio/backend_intro_devops_casino:v1.0.0
          docker push felipensio/backend_intro_devops_casino:sha-${{ github.sha }}

  deploy:
    needs: push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: echo "Despliegue simulado en producción completado."